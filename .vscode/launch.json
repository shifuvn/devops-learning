{
	"configurations": [
		{
			"name": "Docker Node.js Launch",
			"type": "docker",
			"request": "launch",
			"preLaunchTask": "docker-run: debug",
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/docker-swarm/node/package.json",
				"localRoot": "${workspaceFolder}/docker-swarm/node"
			}
		}
	]
}
